// MATH FEATURE

// multiple feature
const multiply = (x, y) => x * y;

// addition feature with brackets on function
const addition = (x, y) => {
  return x + y
};

// console log feature
console.log('Feature release');
